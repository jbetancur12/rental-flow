import express from 'express';

const router = express.Router();

// Placeholder for contract routes
router.get('/', (req, res) => {
  res.json({ message: 'Contracts endpoint - to be implemented' });
});

export default router;