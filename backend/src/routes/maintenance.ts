import express from 'express';

const router = express.Router();

// Placeholder for maintenance routes
router.get('/', (req, res) => {
  res.json({ message: 'Maintenance endpoint - to be implemented' });
});

export default router;